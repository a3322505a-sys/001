# R2–R7 连续执行记录

用户已授权自主执行 R2–R7。依据《吉他App未来规划方案_20260906_0456.md》，从 R1 main `756564b` 接续；各批次独立 feature/PR、沿用现有 CI。完整节奏小节、实琴识别、成就和云端账号不在该方案范围。

| 阶段 | 当前代码状态 | 版本 |
| --- | --- | --- |
| R1 | 已合并：区域折叠、主题和状态 | alpha04 / 21 |
| R2 | 已合并 PR #42，main `af26548`；CI 33999064871 通过 | alpha05 / 22 |
| R3 | 四方向映射闭环已实现 | alpha06 / 23 |
| R4 | 待执行：专项练习 | — |
| R5 | 待执行：和弦显示和逐点任务 | — |
| R6 | 待执行：读谱与区域 | — |
| R7 | 待执行：关系、结构和参照听觉 | — |

## R2

P04–P09 从 P03 的原进度继续；保持每节点两个主学点和原掌握条件，共覆盖低把位 18 个自然音位。后继预学习按真实课程先修查找，P09 不引入额外低把位新音；反向选项池从课程音位生成，正确答案始终在选项中，A 在 P04 的示范后进入正式作答。旧点复习、预学习限频及示范不计独立证据继续有效。

定向验证：18 音位及后继、选项可达；P03 档案接续；原完整学习回路扩展至 P09 并穿插序列化重读。具体 CI 与 main SHA 见该版本 PR 的交付记录。P01–P03 已有用户真机确认，新节点仍待使用反馈。

## R3

mapping 保留 P03 直接前置。固定唱名 C/Do、D/Re、E/Mi、F/Fa、G/Sol、A/La、B/Si 先示范后练习；完成固定唱名双向后进入 C 大调的双向级数。任务存储音名、方向、主音和调式；级数由 MusicFacts.majorDegree 计算。每音名、每语境沿用最近六次至少五对、双方向各至少两次且末次正确的标准，不把一个方向复制给另一个。提示后的反向立即复述不计独立证据。

节点详情呈现真实方向记录；同一 Room 快照保存任务和纠错状态，不新建映射分数库。新增字段可缺省，旧 v2 任务仍可读；无效调性备份拒绝恢复并保留原档案。测试覆盖 P03 入口、完整闭环与重读、C 在 C/G 大调中级数不同、提示反向污染、首错纠正及 Room 恢复不重复。

乐理核验：[Open Music Theory：音阶与级数](https://openmusictheory.github.io/scales.html)。固定唱名按方案采用 Si 表示 B，与可动 Do 的调内唱法分开。

## 后续实施默认边界

- R3：先逐音介绍固定唱名，再练双向映射；级数任务明确主音和大调语境，方向证据分开。
- R4：专项入口收在区域或节点内部，只选已接触范围；有效的单项独立回答可回流对应技能，提示和示范只记接触。组合题按已回答成员记录，不用总正确率替代节点条件。
- R5：共用实体指板和同一形态数据；默认颜色框，可切编号或音名；逐点输入，不宣称识别真实手指或演奏能力。
- R6：中把位和 9–12 品按两点课程配置逐步引入；TAB 固定坐标、五线谱纯音高题接受题目范围内的等价位置。短句不冒称完整小节。
- R7：按局部目标细分先修；以实际音高判断音程，结构以根音为参照，听觉提供参考音，不设无参照绝对音高门槛。

每次数据增量使用原档案的可缺省字段；写入失败保留原状态，任务实例和技能身份分离。视觉与声音效果交付后真机确认，CI 成功不代替真机结果。
