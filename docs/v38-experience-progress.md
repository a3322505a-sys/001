# v38 训练体验施工进度

方案：项目01《01_v38实测问题归因与处理方案_20260907.md》，修订5（2026-09-07 14:25 UTC）。

## P0 基线

远端 main 为 46b28124f3a36d9fca5472e3c92c40278a51bf7c（alpha17）。PR #55–#58 未合并；#58 的 188ddb24493d6fc3c71d959fce3ab27d1962df42 包含 v38 及所有依赖，Android CI 34098074574 成功。新 feature 从 main 创建并快进接入 v38，未重复实现旧方案。

当前链路 MainActivity → LearningApp → TrainingScreen → TeachingFretboard；Room schema 1 保持。

## P1 纠错减负

正确目标显示音名；在实际 introductions 中选最近同弦参照，最多一处。新题、旧快照统一投影短反馈，不截取历史 explanation。纠错曝光使用同一显示投影，序列不再因完整旧文案而曝光未显示的后续成员。主动示范仍保留完整说明。新增边界回归和固定图3预览，复用原CI。

版本 alpha22 / 39；本地 Gradle 下载网络不可达，构建与模拟器预览待现有 CI。真机阅读效果待用户确认。

## P2 轮次与退出

区域轮次为3热身+7主练+2收尾；每个任务占一题，纠错和重听不加题。第12题纠正反馈完成后自动结束，不生成第13题。返回等待正在进行的作答保存，失败后重试先保存事实再结束；只结算绑定会话。后台仍保留本轮，主动结束后创建新会话并沿用原降级配置和恢复证据，保留其他暂停上下文。旧格式字段均可缺省，不改Room表结构。短谱试点仍按完整段落与原自评流程处理。

版本 alpha23 / 40。补充有限轮次、暂停区域、保存中退出与失败重试、结束事务回滚重开检查；旧无限循环调度测试改为跨轮继续，保留原学习推进断言。P2当前代码待CI验证。

## 待实施差集

P3：所有题位负荷限制、逐目标撤辅助及跨轮恢复。
P4：首帧有效计时、手势归属和即时长考保护。
P5：轮末A/W、热身升级、近期快速稳定判据。
P6：覆盖/保持/速度准入及中把位渐进推荐。
P7：统一指板器件几何、标记及命中。
