# 01 指板质感与训练停滞：alpha25 工作分支记录

依据：01_指板质感与训练停滞施工方案_20260908.md 修订2（2026-09-08 05:38 UTC+8）、本轮用户“查找文档，自主推进”、项目规范(1).md 与远端 AGENTS.md。

## 基线和实际状态

- 仅仓库 a3322505a-sys/001。重新读取远端 main 为 24e02f7caac8df0aefb829b81559300b225bb1c7；PR #59/#60 已合并；main CI 34161659803 成功。相关现有分支未发现本方案新实现。
- 分支 feature/training-progression-visuals-20260908；目标版本 2.0.0-alpha25 / versionCode 42。前一正式版本为 alpha24 / 41。
- 本批已提交 [PR #61](https://github.com/a3322505a-sys/001/pull/61)，最新 CI、合并、正式 APK 和截图验收以该 PR 的交付记录为准；真机结果单独确认。
- 初次推送被自动审批拒绝后，用户明确授权推送、PR、CI及通过后的合并打包。Git命令行无登录凭据，授权后经已连接的GitHub接口提交，文件树与本地一致。
- 本地 ./gradlew testDebugUnitTest --offline 也无法启动：Gradle 8.9 分发包未缓存，下载网络不可达。不是测试失败，也不是测试通过。

## 需求—实现—待验收

| 范围 | 本地改动 | 必须继续的验收 |
| --- | --- | --- |
| P0 核验 | 远端分支、最近 PR、main CI、规范与当前入口已读；确认少数保护项抢占、二选一收缩、间隔目标不足和固定第7题机制。无用户档案，不声称复现手机问题。 | 运行真实 Kotlin 协调器基线/修改后用例，记录实际逐题结果。 |
| P1 七项与恢复 | 自然音认音统一 C/D/E/F/G/A/B；补练改为目标集中/找位范围/必要示范；所有保护项轮转；小池可短教第三目标以提供真实间隔。2次及时成功辅助练习后撤辅助，穿插其他位置，3次及时无辅助间隔复测恢复；孤立误触保留1次复测。成功复测不要求重新完成4道辅助题。 | 1/2/3个已教点及多项保护真实恢复、再次失败、跨轮重载、存储失败。候选规则尚未经实测校准。 |
| P2 本轮推进 | 4个有效快准首答、至少2位置及双方向覆盖支持上探；认音≤3秒、找位≤5秒，排除提示、重听、中断、可疑快触。新点示范后双方向验证再上探；12题边界保留。区域试练先修可由已教位置链满足，不写假课程通关。中区试练要求低区18点双向最新及时首答覆盖；长期熟练度仍保留原保持验证。 | 第一轮实际出现更大范围的题、5–6轮覆盖、慢错与缺少计时反例。没有运行结果前不宣称训练停滞已解决。 |
| P3/P4 指板样段与接入 | 在原二维绘制中改进不规则木纹、琴头边缘、凹槽、金属弦轴结构与反射；保持现有指板几何、六弦和标记命中路径。 | 本轮仅取得方案文档，未取得原琴头参考原图；未做原型截图审阅。需要 CI 固定状态截图及参考对照，再确认全范围视觉接入；不能宣称已达到参考质感。 |
| P3/P5 和弦图 | 局部指板、品号、O/X、固定颜色与1–4数字，横按连续条；横图上1下6，竖图左6右1；同一弦品事实投影，文字不旋转。训练与示例页旋转入口、方向偏好沿原事务保存；示例页横屏，保持形态与试听入口。 | 两方向显示/点击、切换两次、不中断进度、不自动重播、小屏大字和高起始品位截图，以及偏好保存失败/重启。 |

## 数据保护和实现边界

- 包名 com.a3322505a.guitarlearning、Room schema 1、原签名策略均保持；未接触任何用户学习档案或签名私钥。
- 新题归一为七项；只在未作答安全边界重建旧暂停题，保留 taskId 并把该题计时标为中断。已回答/部分回答任务和历史成绩不改写。
- 历史两项或不足七项的认音证据不冒充新七项熟练度/推进/恢复证据；旧长考字段仍按原结构合法读取。
- 新偏好 chordVertical 默认 false，缺省兼容；复用 TrainingViewModel.change / RoomLearningRepository 事务，失败保留旧快照。
- ChordDiagram 只接显示契约、输出 PositionTapped；没有把训练状态、判题或播放器传入显示组件。没有新增和弦库、升降号课程、Room schema、CI gate。

## 验证资产（均待执行，不能当作通过证据）

ProgressionVisualsTest：首轮上探、多轮覆盖、无效/慢计时反例、微小题池/多项保护恢复、旧快照成绩隔离、两方向弦品往返与偏好缺省。

LearningRepositoryTest 新增旋转偏好写入失败回滚、重启保留和任务历史一致；沿用已有保护/长考事务验证。按本次规则更新 AdaptiveDowngradeTest、ExperiencePolicyTest 和 RegionTrainingTest 的旧策略断言。

UiPreviewTest 增加开放和弦竖图、横按横/竖图及大字采集，七项补练状态也已更新；沿用原手工截图流程，不新增门槛。

本地 git diff --check 通过。其结果只说明补丁无空白错误，不说明 Kotlin 编译、运行或学习有效性。

## 下一步

用户已明确授权上述全流程。每次交付前重新核验远端 main 与分支占用；必要时合并最新 main 并处理冲突，运行现有 Android CI。修复所有真实编译/测试失败，审阅固定状态截图，随后依当前授权完成合并和原长期签名 APK。记录源 SHA、CI、APK 包名/版本/签名与 hash；真机效果另记。

实际页面的普通题与补练均启动独立作答计时；补练计时只参与撤辅助，不计稳定熟练度。TrainingAudioIntegrationTest 覆盖实际 ViewModel 显示→计时→保存，以及旋转前后的任务、首播归属一致。
