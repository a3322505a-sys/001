# P3 + P6 整合 · alpha21

2026-09-07。用户确认 v37 没有包含 v36 后，授权将两版整合交付。

## 源码范围

- 开工远端 main 为 `46b28124f3a36d9fca5472e3c92c40278a51bf7c`；PR #56 与 #57 均未合并。
- 整合提交保留两个父提交：P6 的 `ca295811884e754039a3f2a2262277f123abfa0f` 与 P3 返工的 `62a58464a98cd1d84428f72c512cb8d64962da44`。两者共同基线为 PR #55 的 `668f292e9aacf4600bd416235d0adce330ab4679`。
- 版本递增为 `2.0.0-alpha21` / `38`；沿用原包名、Room schema 1 和长期签名。历史 alpha19/20 文档保留各自交付时的事实。

## 合并边界

- P3 纯音位的即时首答触发、局部两选项/缩范围、撤辅助后原条件复测全部保留。`scaffolded` 题不进入熟练度和恢复样本，也不增加原课程掌握。
- P6 保留读谱、和弦、关系、听辨、转换的逐成员首答条件、题型配置与减量恢复。首答事件读取成员自己的辅助状态，纠错展示后的未独立成员不能借用整题首答状态成为证据。
- 共用薄弱点投影时，区域音位/映射读取即时信号；带 `familyScope` 的题与 `family:` 单元继续读取有间隔的 P6 样本。配置窗口、方向、调性与保持检查沿用已有实现。
- `familyScope` 与 `scaffolded` 同时保留，缺省值兼容各自旧档案；两边的任务和配置校验同时执行。区域降级与已暂停的题型任务存于同一档案，可往返恢复各自原题和配置。

## 验证与交付

- 完整保留两版现有测试，包括 Room 写入失败回滚和无效备份保护。
- 增补跨模块路径：P6 已降级暂停 → P3 连续首错并简化 → 档案重载 → 返回 P6 原题 → 再进 P3 原题；检查两个配置和成员证据不丢失。
- 检查 v36 缺少辅助字段、v37 缺少题型字段时恢复，以及即时信号不计入纠错展示后的成员。
- 复用现有 Android CI、升级保留档案与音频冒烟，不增加新 CI gate。CI、源码 SHA 和 APK 摘要以整合 PR 的最终交付记录为准。真机难度体感与听感需实际试用。
