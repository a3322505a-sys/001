# alpha02 指板反馈修正

> 状态：已实现并合并（PR #38），保留为版本记录。当前版本及接手位置见 [README](../README.md)；实际指板入口为 `learning/TeachingFretboard.kt`。

针对 alpha01 真机反馈，保留 Draft 0.4 A+B 学习路径与既有学习档案。

- 六枚直列弦钮、曲线枫木琴头、弦枕、木纹、金属品丝与粗细/绕弦细节。
- 取消指板常驻弦号、品号以及底部坐标说明。G00 入门示范只临时标注当前弦或品，独立练习不显示编号；音位示范同样不显示坐标尺。题目文字、TAB 数字及读屏语义仍保留其教学用途。
- 题目位置使用亮青蓝色、黑白描边；整弦/整品用同色填充和边框。正确、错误用独立颜色和勾/叉区分。
- 所有可点击格与绘图共用原有品位比例。旧档案内已保存的题目也立即应用新显示规则，旧文案只在显示时转换，不改题目身份、答题记录或证据。
- versionName 2.0.0-alpha02，versionCode 19，沿用 alpha01 的包名、私有签名密钥、数据库 schema 1。
- 原有 CI 覆盖升级检查改为按基础 APK 版本递增，不再锁定 18→19；没有新增测试门槛。

验证由原有 Android CI 构建、测试及覆盖安装检查完成。最终布局与材质观感以用户真机反馈为准。
